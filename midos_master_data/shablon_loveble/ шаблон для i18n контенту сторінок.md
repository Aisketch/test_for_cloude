### 3️⃣ **MECE шаблон для i18n контенту сторінок**

```markdown
# Content Structure (i18n Ready)

## How to use this file
1. Fill content for BOTH languages (en/ua) in parallel
2. Keep keys identical (e.g., `hero.title`)
3. Add this file to Knowledge Base OR create separate JSON files

## Alternative Format: JSON Structure
(Якщо ви хочете окремі JSON файли замість таблиць)

**File:** `content/i18n/en.json`
```json
{
  "hero": {
    "badge": "AI-Powered Platform",
    "title": "Smart booking that adapts to you",
    "description": "Experience seamless appointment scheduling...",
    "cta": {
      "primary": "Start booking",
      "secondary": "Watch demo"
    },
    "microDetails": [
      "Real-time sync",
      "Mobile optimized",
      "Zero setup"
    ]
  },
  "features": {
    "heading": "Why choose us",
    "cards": [
      {
        "title": "Instant booking",
        "description": "Book appointments in seconds..."
      }
    ]
  },
  "nav": {
    "home": "Home",
    "features": "Features",
    "pricing": "Pricing",
    "about": "About",
    "login": "Login"
  }
}
```

**File:** `content/i18n/ua.json`
```json
{
  "hero": {
    "badge": "AI-платформа",
    "title": "Розумне бронювання що адаптується до вас",
    "description": "Відчуйте безшовне планування зустрічей...",
    "cta": {
      "primary": "Почати бронювання",
      "secondary": "Дивитись демо"
    },
    "microDetails": [
      "Синхронізація в реальному часі",
      "Оптимізовано для мобільних",
      "Нульове налаштування"
    ]
  }
}
```

---

```

---

### 4️⃣ **Як бути з картинками?**

**MECE структура управління зображеннями:**

```markdown
# Image Management Strategy

## A. Image Sources (Звідки беремо)

### 1. From delphi.ai
**Method:** Screenshot + crop
**Storage:** `public/images/reference/`
**Usage:** Тимчасово для layout, замінити на кастомні

### 2. Stock/AI Generated
**Method:** 
- Unsplash/Pexels (free stock)
- AI generation (Midjourney, DALL-E, Lovable AI може генерувати через gemini-2.5-flash-image)
**Storage:** `public/images/stock/`

### 3. Brand Assets (ваші)
**Method:** Upload через Lovable або Firebase Storage
**Storage:** `public/images/brand/`
**Files:** logo, team photos, custom graphics

### 4. Icons
**Method:** lucide-react (already installed) або react-icons
**Storage:** Import from package
**Example:** `import { Zap } from "lucide-react"`

---

---

### 6️⃣ **Як прибирати баги/лишні файли перед Firebase?**

**MECE підхід до підготовки проекту:**

```markdown
# Pre-Firebase Migration Checklist

## A. Cleanup Strategy

### 1. Remove Lovable-specific files
**What to remove:**
```
✅ Remove:
- /.lovable/ (internal Lovable config)
- lovable-tagger (dev dependency, check package.json)
- Any .lovable.* files

✅ Keep:
- All src/ files (your app code)
- All public/ files (assets)
- package.json, vite.config.ts (needed for build)
- .env structure (but update values for Firebase)
```

**Method:**
```bash
# Before git push to your Firebase repo:
rm -rf .lovable/
# Edit package.json: remove "lovable-tagger" from devDependencies
# Edit vite.config.ts: remove componentTagger() import and usage
```

---

### 2. Update environment variables
**Current (.env):**
```bash
VITE_SUPABASE_URL=https://gxdtjioclrvgbvssklvq.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc...
```

**For Firebase (.env.production):**
```bash
# Replace Supabase with Firebase config
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123

# Remove all VITE_SUPABASE_* variables
```

---

### 3. Replace Supabase client with Firebase
**Current:** `src/integrations/supabase/client.ts`

**New:** `src/lib/firebase.ts`
```typescript
import { initializeApp } from 'firebase/app';
import { getAuth } from 'firebase/auth';
import { getFirestore } from 'firebase/firestore';
import { getStorage } from 'firebase/storage';

const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN,
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID,
  storageBucket: import.meta.env.VITE_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID,
  appId: import.meta.env.VITE_FIREBASE_APP_ID
};

const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);
export const db = getFirestore(app);
export const storage = getStorage(app);
```

**Update imports across codebase:**
```typescript
// OLD:
import { supabase } from "@/integrations/supabase/client";

// NEW:
import { auth, db, storage } from "@/lib/firebase";
```

---

### 4. Remove unused dependencies
**Check package.json:**
```json
{
  "dependencies": {
    // ❌ Remove if not using:
    "@supabase/supabase-js": "^2.81.0",  // Replace with Firebase

    // ✅ Keep if using:
    "@tanstack/react-query": "^5.83.0",  // Good for data fetching
    "react-router-dom": "^6.30.1",       // Navigation
    "lucide-react": "^0.462.0",          // Icons
    "gsap": "^3.13.0",                   // Animations
    "three": "^0.181.1",                 // 3D hero
    // ... all UI libs (@radix-ui/*)
  },
  "devDependencies": {
    // ❌ Remove:
    "lovable-tagger": "^1.1.11"
  }
}
```

**Add Firebase:**
```bash
npm install firebase
```

---

## B. Build & Deploy Process

### Step 1: Build for production
```bash
# Clean previous builds
rm -rf dist/

# Build optimized version
npm run build

# Preview locally
npm run preview
```

**Check `dist/` folder:**
```
dist/
├── index.html
├── assets/
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── ...
└── images/  (from public/)
```

---

### Step 2: Firebase Hosting setup
**File:** `firebase.json`
```json
{
  "hosting": {
    "public": "dist",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ],
    "headers": [
      {
        "source": "**/*.@(jpg|jpeg|gif|png|svg|webp)",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "max-age=31536000"
          }
        ]
      },
      {
        "source": "**/*.@(js|css)",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "max-age=31536000"
          }
        ]
      }
    ]
  }
}
```

---

### Step 3: Deploy
```bash
# First time setup
firebase login
firebase init hosting

# Select:
# - Use existing project (or create new)
# - Public directory: dist
# - Configure as single-page app: Yes
# - Overwrite index.html: No

# Deploy
npm run build && firebase deploy
```

---

## C. Testing Checklist (Before going live)

### Functionality
- [ ] All routes work (no 404s)
- [ ] Authentication flow (if implemented)
- [ ] Database reads/writes (Firebase Firestore)
- [ ] File uploads (Firebase Storage)
- [ ] Forms submit correctly
- [ ] i18n switching works (en ↔ ua)

### Performance
- [ ] Lighthouse score >90
- [ ] Images optimized (WebP format)
- [ ] Lazy loading implemented
- [ ] No console errors
- [ ] Mobile responsive (test all breakpoints)

### SEO
- [ ] Meta tags present
- [ ] robots.txt configured
- [ ] Sitemap generated
- [ ] Open Graph images
- [ ] Canonical URLs

---

## D. Migration Script (Optional)

**File:** `scripts/prepare-firebase.sh`
```bash
#!/bin/bash

echo "🧹 Cleaning Lovable-specific files..."
rm -rf .lovable/

echo "📦 Removing lovable-tagger from package.json..."
# Use jq or manually edit package.json to remove lovable-tagger

echo "🔧 Updating vite.config.ts..."
# Remove componentTagger import and usage

echo "🏗️  Building for production..."
npm run build

echo "✅ Project ready for Firebase deployment!"
echo "Next steps:"
echo "1. Update .env with Firebase config"
echo "2. Replace Supabase client with Firebase in code"
echo "3. Run: firebase deploy"
```

---

## E. Important Notes for Your Project

### Supabase → Firebase Migration
Since you're using **Lovable Cloud (Supabase)**, migrating to Firebase requires:

**Database:**
```typescript
// OLD (Supabase):
const { data } = await supabase
  .from('bookings')
  .select('*')
  .eq('user_id', userId);

// NEW (Firebase):
import { collection, query, where, getDocs } from 'firebase/firestore';
const q = query(
  collection(db, 'bookings'), 
  where('user_id', '==', userId)
);
const snapshot = await getDocs(q);
const data = snapshot.docs.map(doc => doc.data());
```

**Auth:**
```typescript
// OLD (Supabase):
const { user } = await supabase.auth.signUp({ email, password });

// NEW (Firebase):
import { createUserWithEmailAndPassword } from 'firebase/auth';
const userCredential = await createUserWithEmailAndPassword(auth, email, password);
const user = userCredential.user;
```

**Storage:**
```typescript
// OLD (Supabase):
await supabase.storage.from('images').upload(path, file);

// NEW (Firebase):
import { ref, uploadBytes } from 'firebase/storage';
const storageRef = ref(storage, path);
await uploadBytes(storageRef, file);
```

---

### Alternative: Keep Lovable Cloud Backend
**Option:** Deploy frontend to Firebase Hosting, keep backend on Lovable Cloud

**Pros:**
- No migration needed
- Lovable Cloud already configured
- Just deploy `dist/` to Firebase

**Cons:**
- Split infrastructure (might complicate future scaling)

**firebase.json for this setup:**
```json
{
  "hosting": {
    "public": "dist",
    "rewrites": [
      {
        "source": "/api/**",
        "function": "api"  // If you add Cloud Functions later
      },
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
```

Your Supabase backend (gxdtjioclrvgbvssklvq.supabase.co) stays, frontend on Firebase.

---

## F. Recommended Approach

Given you mentioned **"сервіс будується на firebase/google cloud"**, I recommend:

### Phase 1: MVP on Lovable (Current)
- Build entire frontend + backend on Lovable
- Use Lovable Cloud (Supabase) for speed
- Test everything

### Phase 2: Migrate to Firebase
**Week 1: Frontend Only**
- Deploy `dist/` to Firebase Hosting
- Keep Lovable Cloud backend (no code changes)
- Update DNS

**Week 2-3: Backend Migration**
- Migrate database Supabase → Firestore
- Migrate auth Supabase Auth → Firebase Auth
- Migrate storage Supabase Storage → Firebase Storage
- Update all `src/integrations/supabase/*` → `src/lib/firebase.ts`

**Week 4: Full Firebase**
- Cloud Functions for serverless logic
- Firebase Analytics
- Firebase Performance Monitoring

This phased approach minimizes risk!
```

---
### Navigation Menu

| Key | EN | UA |
|-----|----|----|
| nav.solutions | Solutions | Рішення |
| nav.industries | Industries | Індустрії |
| nav.pricing | Pricing | Ціни |
